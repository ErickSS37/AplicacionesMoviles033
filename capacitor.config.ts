import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Hola Mundo AM033 Erick Soriano',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
